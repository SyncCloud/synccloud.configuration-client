{
  "name": "synccloud.configuration-client",
  "description": "Client to configuration manager service",
  "main": "lib/index.js",
  "version": "0.1.0",
  "scripts": {
    "test": "./node_modules/.bin/mocha ./test"
  },
  "dependencies": {
    "colors": "^1.1.2",
    "debug": "^2.2.0",
    "lodash": "^3.10.1",
    "socket.io-client": "^1.3.6"
  },
  "devDependencies": {
    "chai": "^3.2.0",
    "ghooks": "^0.3.2",
    "mocha": "^2.2.5"
  },
  "config": {
    "ghooks": {
      "pre-commit": "npm run test"
    }
  }
}
